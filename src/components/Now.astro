---
import { NOW_IDS } from '../content/now.ts';
import NoteLink from './NoteLink.astro';
import { t as translate, getLocale, type Locale } from '../i18n';

const locale = (Astro.currentLocale ?? getLocale(Astro.url)) as Locale;
const t = (k: string) => translate(k, locale);
---
<section id="now" class="reveal">
  <div class="container">
    <div class="sec-head">
      <span class="eyebrow">{t('home.now.eyebrow')}</span>
      <div>
        <h2>{t('home.now.heading')}</h2>
        <p class="blurb">
          {t('home.now.blurb.pre')}
          <span style="font-style:italic">{t('home.now.blurb.em')}</span>{t('home.now.blurb.post')}
        </p>
      </div>
    </div>
    <div style="border-top:1px solid var(--rule)">
      {NOW_IDS.map((id) => (
        <div class="now-row">
          <div class="k">{t(`home.now.items.${id}.k`)}</div>
          <div class="v">
            {id === 'running' ? (
              <>
                {t('home.now.items.running.lead')}{' '}
                <NoteLink slug="self-hosted-rag-claude-max">OpenKB</NoteLink>{' '}
                {t('home.now.items.running.middle')}{' '}
                <NoteLink slug="mcp-workstream">MCP</NoteLink>{t('home.now.items.running.tail')}
              </>
            ) : t(`home.now.items.${id}.v`)}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
